---
  post:
    200:
      description: |
        Returns a post.
      schema:
        $ref: schemas.yml#/post
      examples:
        application/json:
          $ref: examples.yml#/post
  posts:
    200:
      description: |
        Returns a list of posts.
      schema:
        type: array
        items:
          $ref: schemas.yml#/post
