displayName: foo
description: |
  My description [here](#foo.{foo_id}.post)
  1. my item in the list
    section 1
  2. my item in the list
    section 2
  3. my item in the list

/{foo_id}:
  post:
    displayName: foo
    responses:
      201:
        body:
          application/json:
            schema: !include ../schemas/foo/item.json

  /bar:
    get:
      displayName: foo
      responses:
        201:
          body:
            application/json:
              schema: !include ../schemas/foo/item.json
              example: !include ../schemas/foo/item.example.json

  /baz:
    get:
      displayName: foo
      responses:
        201:
          body:
            application/json:
              schema: !include ../schemas/foo/item.json
              example: !include ../schemas/foo/item.example.json
