#%RAML 0.8
---
baseUri: foo
title: foo
version: foo

documentation:
  -
    title: foo
    content: |
      ## Hello

      World
      ```sh
      curl -X GET -H "someurl"
      ```

      !!!
      ```json
      {
        "key": "value"
      }
      ````
      The extra "`" is intentional

      ```
      # no type
      ```

/foo:
  # /foo: !include resources/foo.raml
  displayName: foo

  /{foo_id}:
    post:
      displayName: foo
      responses:
        201:
          body:
            application/json:
              schema: |
                {
                  "type": "object",
                  "properties": {
                    "a": {
                      "type": "string"
                    }
                  }
                }
